Feature: Selecting StartOver button

  Scenario: Using start over button
    Given the user has selected start over button
    When user enters field values
    Then the user has clicked start over button
